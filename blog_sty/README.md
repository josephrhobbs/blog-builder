# Blog Builder Stylesheets

This crate provides stylesheets to the Blog Builder.

## Application

This crate enables the Blog Builder to have built-in styles.

Avalilable built-in styles are:
- Technology (`style = "tech"`)